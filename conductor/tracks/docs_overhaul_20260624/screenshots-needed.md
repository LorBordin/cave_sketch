# 📸 Screenshots Needed — Maintainer Action

The docs in this track reference screenshots that **do not exist yet**. The new
web-feature images must be captured by you. Drop each file at the path below and
remove the matching `<!-- SCREENSHOT: ... -->` placeholder comment from the doc.

## New images to capture → save in `docs/web/imgs/`

| File | What to capture | Used in |
|------|-----------------|---------|
| `merge-ui.png` | The Survey Plot merge section: child map/section upload + parent & child station-ID fields + the section-protocol selector | `docs/web/survey-merging.md` (+ `.it.md`) |
| `merge-mirror.png` | A section rendered with the **Simple Mirror** protocol | `docs/web/survey-merging.md` (+ `.it.md`) |
| `merge-displacement.png` | A section rendered with the **Displacement** protocol, showing the two thin connector lines | `docs/web/survey-merging.md` (+ `.it.md`) |
| `satellite-multi-survey.png` | A satellite view with **multiple surveys** drawn after JSON re-import | `docs/web/satellite-maps.md` (+ `.it.md`) |
| `kmz-in-locus.png` | The exported **KMZ opened in Locus Map** (offline) | `docs/web/satellite-maps.md` (+ `.it.md`) |

## Optional — Android

The Android user guide reuses existing shots in
`docs/mobile-app/screenshots_v1/` (survey_1–4, satellite_1–3, about). If you want
**updated** screenshots that reflect the Phase 4 visual redesign, supply them and
we'll add a `docs/android/imgs/` folder and update the references.

## Already available (no action needed)

- `imgs/` — original TopoDroid DXF-export flow (`map_export.jpg`,
  `export_format.jpg`, `section_export.jpg`, `topodroid_icon.png`, `logo.png`).
- `docs/mobile-app/screenshots_v1/` — current Android app screenshots.
